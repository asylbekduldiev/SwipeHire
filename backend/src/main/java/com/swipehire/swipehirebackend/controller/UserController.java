package com.swipehire.swipehirebackend.controller;import com.swipehire.swipehirebackend.model.User;import jakarta.servlet.http.HttpServletRequest;import org.springframework.security.web.csrf.CsrfToken;import org.springframework.web.bind.annotation.GetMapping;import org.springframework.web.bind.annotation.PostMapping;import org.springframework.web.bind.annotation.RequestBody;import org.springframework.web.bind.annotation.RestController;import java.util.ArrayList;import java.util.List;@RestController // – Marks this class as a REST controller, returning JSON data directly.public class UserController {  private List<User> users = new ArrayList<>(List.of(    new User("Alex", 20, 1),    new User("Era", 24, 2)  )); // – In-memory list of users (mock database).  @GetMapping("/users")  public List<User> getUsers(){    return users;  } // – Handles GET requests to /users. Returns the current list of users.  @GetMapping("/csrf-token")  public CsrfToken getCsrfToken(HttpServletRequest request){    return (CsrfToken) request.getAttribute("_csrf");  } // – Handles GET requests to /csrf-token. Extracts the CSRF token from the request (used in forms for security).  @PostMapping("/users")  public User addUser(@RequestBody User user){    users.add(user);    return user;  } // – Handles POST requests to /users. Accepts a new user in the request body (JSON), adds it to the list, and returns it.}